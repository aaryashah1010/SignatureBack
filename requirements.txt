fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
alembic==1.14.0
asyncpg==0.30.0
psycopg2-binary==2.9.10
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
pydantic==2.10.3
pydantic-settings==2.7.0
email-validator==2.2.0
python-multipart==0.0.20
redis==5.2.1
pypdf==5.1.0
reportlab==4.2.5
Pillow==11.0.0
# ── External integration ──────────────────────────────────────────────────────
# Async HTTP client for external callbacks with retry.
httpx==0.28.1
# SQL Server ODBC driver wrapper (requires ODBC Driver 17/18 on the host OS).
# Install with: pip install pyodbc  (and the OS-level ODBC driver)
pyodbc==5.2.0
# 3DES encryption used by CpaDesk for document path encryption/decryption.
pycryptodome==3.21.0
